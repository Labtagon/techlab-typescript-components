import * as mx from "@labtagon/matrix42-workspace-management-types";

export default class SlideToUnlockService {
    static $inject: string[] = [

    ]

    constructor(
        private publicService: mx.sb.public.service
    ) {

        
    }
}